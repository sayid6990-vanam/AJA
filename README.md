# AHAD JUS DUX — iOS Liquid Glass Background Fix

This version keeps the uploaded city images visible behind the whole website:

- `dark-bg.png` is used in dark mode.
- `light-bg.png` is used in light mode.
- The hero section has been patched so the background image is not hidden by a solid navy layer.
- The hero logo card and floating cards have been resized and repositioned to avoid clipping.
- The liquid-glass panels are more transparent and blend with the city background.

Upload all files directly to the GitHub repository root:

```text
README.md
index.html
style.css
script.js
logo.png
intro.mp4
dark-bg.png
light-bg.png
```

After upload, redeploy on Netlify using **Trigger deploy → Clear cache and deploy site**.
